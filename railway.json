{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "daphne home_services.asgi:application --port $PORT --bind 0.0.0.0",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}